## Quick notes

1. Internal secrets generated by me to have CF tunnel and secure connections between services
   use openssl rand -base64
   "INTERNAL_API_SERVICE_SECRET"
   "WORKFLOW_SERVICE_SECRET"

```bash
openssl rand -base64 32
```
