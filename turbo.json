{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "stream",
  "globalDependencies": ["tsconfig.base.json", "pnpm-workspace.yaml"],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "typecheck": {
      "dependsOn": ["^typecheck"],
      "outputs": ["dist/**", "*.tsbuildinfo"]
    },
    "lint": {
      "outputs": []
    },
    "test": {
      "dependsOn": ["^typecheck"],
      "outputs": []
    },
    "build": {
      "dependsOn": ["^typecheck", "^build"],
      "outputs": ["dist/**"]
    },
    "@strengthsync/client#build": {
      "dependsOn": ["^typecheck", "^build"],
      "outputs": ["dist/**"],
      // Inlined into the bundle, so it is part of what the output depends on.
      // Turbo infers VITE_* from the framework, but naming it keeps the
      // guarantee visible: leave it out and a cached bundle built before the
      // key existed is still a legal answer for a build that has it.
      "env": ["VITE_POSTHOG_KEY"]
    },
    "@strengthsync/server#build": {
      "dependsOn": ["^typecheck", "^build", "@strengthsync/client#build"],
      "outputs": []
    },
    "deploy": {
      "dependsOn": ["build"],
      "outputs": [],
      "cache": false,
      "passThroughEnv": ["CLOUDFLARE_API_TOKEN", "CLOUDFLARE_ACCOUNT_ID"]
    }
  }
}
